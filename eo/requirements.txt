# Core STAC + Planetary Computer
pystac-client==0.8.5
planetary-computer==1.0.0

# Geometry (matches root backend versions to avoid resolver conflicts)
shapely==2.0.6
pyproj==3.7.0

# Raster IO + processing — read windowed bands directly from
# Planetary Computer's signed COG URLs, no full-tile download.
rasterio==1.4.3
